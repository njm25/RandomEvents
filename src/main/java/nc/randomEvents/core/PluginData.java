package nc.randomEvents.core;

public interface PluginData {
    /**
     * Returns a unique identifier for this data instance
     * Used as the key for storage and retrieval
     */
    String getId();
}
